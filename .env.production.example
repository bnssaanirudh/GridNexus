# Copy to .env.production or export through your secrets manager.
# Never commit the populated file.
POSTGRES_PASSWORD=replace-with-a-long-random-password
REDIS_PASSWORD=replace-with-a-long-random-password
JWT_SECRET=replace-with-at-least-32-random-characters
ENGINE_JWT_SECRET=replace-with-a-different-32-character-secret
ENCRYPTION_KEY=replace-with-exactly-64-hex-characters
CORS_ORIGINS=https://gridnexus.example.com
LLM_API_KEY=

